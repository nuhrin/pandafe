NULL =

scriptsdir=$(pkgdatadir)/scripts

scripts_SCRIPTS = \
	cpuspeed_change_exec_wrapper.sh \
	$(NULL)

dist_scripts_DATA = \
	cpuspeed_change_exec_wrapper.sh \
	$(NULL)

EXTRA_DIST = \
	update-stamp-from-dependencies.sh \
	$(NULL)

cpuspeed_change_exec_wrapper.sh: \
	$(NULL)
